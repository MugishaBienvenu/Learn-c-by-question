// 2.1 For each of the declarations below, do the following. State whether the declaration is also a definition. If the
// declaration is not a definition, write a definition for the entity being declared. If the declaration is a definition,
// write a declaration for the entity being declared that is not also a definition.
// (a) int i = 5;
// (b) int abs(int x);
// (c) float sqr(float x) {return x * x;}
// (d) extern const double pi;
// (e) char s[] = "Hello";
// (f) double x;
// (g) int (*func)(int, int);
// (h) template <typename T> T max(T x, T y);
// (i) template <typename T> bool is_negative(T x) {return x < 0;}
// (j) auto meaning_of_life = 42;

                    //   answer of question 
// a) int i =5; this statement represent declaration of integer ,int is type of data uc=sed to declalre integer number only .
// this is example of int {int i =5;}.
// b)int abs(int x); : in oop abstraction ccan hiding data in class . this variable can hide in class 
// class Calc{
    //   private:
    // int x 
    //}.
    // c)float sqr(float x) {return x*x} float used to declare decimal nummbers have variable called sqr that can return x power two in 
    // code when you declare variable sqr  and float are used on lower pricision than double 
    // d)extern const double pi; declaration of pi can not change because of const datatype used to return variable in constant form ,then variable can 
    // not change example :const double pi;
// e)char s[] = "hello"; : this datatype can store character only in single or double coutes.
//     s is variables that contain value called hello in form of array.
//f)double x : it is declaration of variable x that can have high precision of decimal number,and it used to declare variable that can have 
// high decimal number example double x=2.45678;
//g)int (*func)(int,int): *func is pointer that can contain address of int like int ,int, *func is used to declare pointer
// example : int x=21;
//           int* ptr= &x;
// h)template <typename t> t max(t x, t y) : max keyword are used to display high number in different number example : max(3,6);
// i)template <typename t> bool is_negative(t x){return x<0} bool is datatype that can return true or false there x is true because x are less than 0
// x is negative
// j) auto meaning_of_life=42; : auto is datatype used to declare variable in c++ example : auto a;





