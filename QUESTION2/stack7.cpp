// 3.7 Develop a class IntStack that represents a stack containing int elements. Provide the basic functionality that
// would normally be expected from a stack (e.g., push an element on the stack, pop an element off the stack, get
// the top element on the stack, query the number of elements on the stack). The std::vector class template may
// be used to hold the elements on the stack.

#include <iostream>
#include <stack>
#include<vector>
using namespace std;


// class IntStack{
//    public:
//    vector<int> x;

//   void stack(int y){
//     if(x.empty()){
//         cout<<x.top();
//         x.pop
//     }
//   }

 
// };

int main (){
    // IntStack intstack;
    // intstack.stack(12);
    // stack<int> x;
    // x.push(1);
    // x.push(2);
    // x.push(3);

 string arr[]={"banana","mango","okiok"};

   int length=sizeof(arr)/sizeof(arr[0]);

    cout<<length;

    return 0;
}
